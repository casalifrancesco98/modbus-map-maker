/* Auto-generated by modbus-map-maker */
#ifndef MODBUS_MAPPING_H
#define MODBUS_MAPPING_H

#ifdef __cplusplus
extern "C" {
#endif

typedef struct {
  const char* device;
  const char* name;
  int address;
  const char* dtype;
  float scale;
  float offset;
  const char* unit;
  const char* function;
  const char* byte_order;
  const char* word_order;
} modbus_entry_t;

static const modbus_entry_t MODBUS_MAP[] = {
{% for e in entries -%}
  {"{{ e.device }}","{{ e.name }}",{{ e.address }},"{{ e.dtype }}",{{ e.scale }},
   {{ e.offset }},{{ '"%s"' % e.unit if e.unit else 'NULL' }},"{{ e.function }}",
   "{{ e.byte_order }}","{{ e.word_order }}"},
{% endfor -%}
};

static const int MODBUS_MAP_SIZE = sizeof(MODBUS_MAP) / sizeof(MODBUS_MAP[0]);

#ifdef __cplusplus
}
#endif
#endif
